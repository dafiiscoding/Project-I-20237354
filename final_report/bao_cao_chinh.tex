% =====================================================================
%  BÁO CÁO ĐỒ ÁN I — DỰ BÁO RỦI RO VỠ NỢ TÍN DỤNG BẰNG MACHINE LEARNING
%  Tác giả: Đoàn Danh Long — 20237354 — Khoa Toán-Tin, ĐHBK Hà Nội
%  File này được VIẾT TAY (không qua pandoc). Biên dịch: xelatex 2 lần.
%  Build: python build_pdf.py   (hoặc latexmk -xelatex bao_cao_chinh.tex)
% =====================================================================
\documentclass[12pt,a4paper,oneside]{report}

% ---------- Font & ngôn ngữ ----------
\usepackage{fontspec}
\setmainfont{Times New Roman}
\newfontfamily\monofont{Cascadia Mono}
\defaultfontfeatures{Ligatures=TeX}

% ---------- Hình học trang ----------
\usepackage[a4paper,margin=2.5cm,headheight=15pt]{geometry}
\usepackage{setspace}
\onehalfspacing

% ---------- Toán ----------
\usepackage{amsmath,amssymb}
\usepackage{mathtools}
\usepackage{amsthm}

% ---------- Màu & box ----------
\usepackage{xcolor}
\definecolor{bkblue}{RGB}{0,51,102}      % xanh navy tiêu đề
\definecolor{bkred}{RGB}{197,24,24}       % đỏ nhấn
\definecolor{boxgray}{RGB}{247,247,247}
\definecolor{linkcol}{RGB}{0,71,133}
\usepackage[most]{tcolorbox}

% ---------- Định lý / Định nghĩa (kiểu box như báo cáo tham khảo) ----------
\newtheoremstyle{vnplain}{6pt}{6pt}{\itshape}{}{\bfseries\color{bkblue}}{.}{.5em}{}
\theoremstyle{vnplain}
\newtheorem{dlytheo}{Định lý}[chapter]
\newtheorem{menhde}[dlytheo]{Mệnh đề}
\newtheorem{bode}[dlytheo]{Bổ đề}
\newtheoremstyle{vndef}{6pt}{6pt}{}{}{\bfseries\color{bkred}}{.}{.5em}{}
\theoremstyle{vndef}
\newtheorem{dinhnghia}[dlytheo]{Định nghĩa}
\newtheorem{nhanxet}[dlytheo]{Nhận xét}
\newtheorem{vidu}[dlytheo]{Ví dụ}
\renewcommand{\qedsymbol}{$\blacksquare$}
\renewcommand{\proofname}{Chứng minh}
\renewenvironment{proof}[1][\proofname]{\par\noindent\textit{#1.}\ }{\hfill$\blacksquare$\par\vspace{4pt}}

% Box bao quanh định nghĩa/định lý quan trọng
\newtcolorbox{khungtheo}[1][]{enhanced,boxrule=0.6pt,colback=boxgray,colframe=bkblue,
  arc=2pt,left=8pt,right=8pt,top=6pt,bottom=6pt,breakable,#1}

% ---------- Đồ thị, bảng, hình ----------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{caption}
\captionsetup{font=small,labelfont={bf,color=bkblue},justification=centering,skip=6pt}
\usepackage{float}
\usepackage{placeins}
\renewcommand{\arraystretch}{1.25}

% ---------- TikZ (sơ đồ pipeline / kiến trúc) ----------
\usepackage{tikz}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit,backgrounds,calc}

% ---------- Header / Footer ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\small\itshape\nouppercase{\leftmark}}
\fancyhead[R]{\small\thepage}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\small\thepage}\renewcommand{\headrulewidth}{0pt}}

% ---------- Kiểu Chương (số trong khung như báo cáo tham khảo) ----------
\usepackage{titlesec}
\titleformat{\chapter}[hang]
  {\Huge\bfseries\color{bkblue}}
  {\colorbox{bkblue}{\color{white}\rule[-6pt]{0pt}{28pt}\,\thechapter\,}}{16pt}{\Huge}
\titlespacing*{\chapter}{0pt}{10pt}{24pt}

% ---------- Nhãn tiếng Việt ----------
\renewcommand{\chaptername}{Chương}
\renewcommand{\contentsname}{Mục lục}
\renewcommand{\figurename}{Hình}
\renewcommand{\tablename}{Bảng}
\renewcommand{\listfigurename}{Danh sách hình vẽ}
\renewcommand{\bibname}{Tài liệu tham khảo}
\renewcommand{\appendixname}{Phụ lục}

% ---------- Liên kết ----------
\usepackage[unicode,hidelinks]{hyperref}
\hypersetup{colorlinks=true,linkcolor=linkcol,citecolor=bkred,urlcolor=linkcol,
  pdftitle={Dự báo rủi ro vỡ nợ tín dụng bằng Machine Learning},
  pdfauthor={Đoàn Danh Long}}
\usepackage[nameinlink]{cleveref}
\crefname{equation}{công thức}{công thức}
\crefname{figure}{Hình}{Hình}
\crefname{table}{Bảng}{Bảng}
\crefname{chapter}{Chương}{Chương}
\crefname{section}{Mục}{Mục}
\crefname{dlytheo}{kết quả}{kết quả}

% ---------- Lệnh tắt toán ----------
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\xv}{\mathbf{x}}
\newcommand{\wv}{\mathbf{w}}
\newcommand{\sgn}{\operatorname{sign}}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

% =====================================================================
\begin{document}

% =====================================================================
%  TRANG BÌA
% =====================================================================
\begin{titlepage}
\centering
\begin{tikzpicture}[remember picture,overlay]
  \draw[bkblue,line width=1pt,dash pattern=on 6pt off 3pt]
    ($(current page.north west)+(1.4cm,-1.4cm)$) rectangle
    ($(current page.south east)+(-1.4cm,1.4cm)$);
  \draw[bkblue,line width=0.4pt]
    ($(current page.north west)+(1.65cm,-1.65cm)$) rectangle
    ($(current page.south east)+(-1.65cm,1.65cm)$);
\end{tikzpicture}

{\large\bfseries ĐẠI HỌC BÁCH KHOA HÀ NỘI\par}
{\large\bfseries KHOA TOÁN - TIN\par}
\vspace{2pt}{\color{bkblue}\rule{3cm}{0.8pt}}\par
\vspace{1.2cm}

% --- Logo: thả file PNG vào final_report/assets/ là tự hiện ---
\begin{tabular}{c@{\hskip 1.6cm}c}
  \IfFileExists{assets/logo_bachkhoa.png}
    {\includegraphics[height=3cm]{assets/logo_bachkhoa.png}}
    {\fbox{\parbox[c][3cm][c]{3cm}{\centering\small Logo\\ĐHBK}}}
  &
  \IfFileExists{assets/logo_toantin.png}
    {\includegraphics[height=3cm]{assets/logo_toantin.png}}
    {\fbox{\parbox[c][3cm][c]{3cm}{\centering\small Logo\\Toán-Tin}}}
\end{tabular}

\vspace{1.6cm}
{\Large\bfseries BÁO CÁO ĐỒ ÁN I\par}
\vspace{4pt}
{\large HỌC PHẦN ĐỒ ÁN NGHIÊN CỨU\par}
\vspace{1.1cm}
{\color{bkblue}\Huge\bfseries DỰ BÁO RỦI RO VỠ NỢ\par}
\vspace{6pt}
{\color{bkblue}\Huge\bfseries TÍN DỤNG BẰNG MACHINE LEARNING\par}
\vspace{2.2cm}

{\large
\begin{tabular}{r@{\hskip 0.6cm}l}
  \textbf{Giảng viên hướng dẫn:} & \color{bkred}TS. Nguyễn Cảnh Nam\\[4pt]
  \textbf{Sinh viên thực hiện:}  & Đoàn Danh Long\\[4pt]
  \textbf{Mã số sinh viên:}      & 20237354\\[4pt]
  \textbf{Học kỳ:}               & 2025.2\\
\end{tabular}\par}

\vfill
{\bfseries Hà Nội, tháng 6 năm 2026\par}
\end{titlepage}

% =====================================================================
%  PHẦN ĐẦU (đánh số La Mã)
% =====================================================================
\pagenumbering{roman}

% ---------- LỜI MỞ ĐẦU ----------
\chapter*{Lời mở đầu}
\addcontentsline{toc}{chapter}{Lời mở đầu}
\markboth{LỜI MỞ ĐẦU}{}

Hoạt động tín dụng là xương sống của hệ thống ngân hàng, nhưng đi kèm với nó là rủi ro
vỡ nợ — khi người vay mất khả năng trả nợ. Việc đánh giá rủi ro chính xác trước khi cấp
tín dụng quyết định trực tiếp đến chất lượng tài sản và sự an toàn của tổ chức cho vay.
Quy trình thẩm định thủ công truyền thống vừa tốn kém, vừa khó mở rộng quy mô, vừa mang
tính chủ quan; trong khi các mô hình chấm điểm tuyến tính cổ điển (như FICO) bỏ qua phần
lớn các tương tác phi tuyến giữa các yếu tố tài chính.

Học máy (Machine Learning) cung cấp một hướng tiếp cận khác: học quy luật rủi ro trực
tiếp từ dữ liệu lịch sử của hàng trăm nghìn hồ sơ vay, nắm bắt các quan hệ phi tuyến phức
tạp mà mô hình tuyến tính không biểu diễn được. Tuy nhiên, một mô hình tốt trong bài toán
tín dụng không chỉ cần \emph{chính xác} mà còn phải \emph{giải thích được} (do ràng buộc
pháp lý khi từ chối khách hàng) và \emph{phù hợp với chi phí kinh doanh} (vì bỏ sót một
người sẽ vỡ nợ tốn kém hơn nhiều so với từ chối nhầm một người tốt).

Báo cáo này trình bày một cách hệ thống toàn bộ quy trình xây dựng một hệ thống dự báo
rủi ro vỡ nợ trên bộ dữ liệu \emph{Give Me Some Credit} (Kaggle): từ cơ sở lý thuyết của
các mô hình phân loại, khám phá và tiền xử lý dữ liệu, thiết kế đặc trưng, huấn luyện và
so sánh bốn mô hình (Hồi quy Logistic, Cây quyết định, Rừng ngẫu nhiên, XGBoost), đến
diễn giải mô hình bằng giá trị SHAP, tối ưu ngưỡng quyết định theo chi phí, và đóng gói
thành một ứng dụng web hoàn chỉnh. Toàn bộ nội dung được sắp xếp từ cơ sở lý thuyết đến
thực nghiệm và ứng dụng, nhằm tạo thành một bản tổng hợp nhất quán và dễ theo dõi.

% ---------- LỜI CẢM ƠN ----------
\chapter*{Lời cảm ơn}
\addcontentsline{toc}{chapter}{Lời cảm ơn}
\markboth{LỜI CẢM ƠN}{}

Em xin gửi lời cảm ơn chân thành đến Giảng viên hướng dẫn — Thầy Nguyễn Cảnh Nam, Khoa
Toán--Tin, Trường Đại học Bách Khoa Hà Nội — đã tận tình hướng dẫn, góp ý và định hướng
trong suốt quá trình thực hiện đồ án.

Em cũng xin cảm ơn gia đình và bạn bè đã luôn động viên, tạo điều kiện thuận lợi để em có
thể hoàn thành đồ án đúng tiến độ. Dù đã cố gắng hết sức, báo cáo chắc chắn còn nhiều
thiếu sót. Em rất mong nhận được nhận xét và góp ý từ Thầy để tiếp tục cải thiện trong
những nghiên cứu tiếp theo.

\vspace{8pt}
\hfill\emph{Hà Nội, tháng 6 năm 2026 — Đoàn Danh Long}

% ---------- TÓM TẮT ----------
\chapter*{Tóm tắt}
\addcontentsline{toc}{chapter}{Tóm tắt}
\markboth{TÓM TẮT}{}

Báo cáo trình bày nghiên cứu ứng dụng học máy vào bài toán dự báo rủi ro vỡ nợ tín dụng,
sử dụng bộ dữ liệu \emph{Give Me Some Credit} (Kaggle, 149.999 hồ sơ vay). Bốn mô hình
được xây dựng và đánh giá: Hồi quy Logistic ($\mathrm{AUC}=0{,}8432$), Cây quyết định
($0{,}8579$), Rừng ngẫu nhiên ($0{,}8703$) và XGBoost ($0{,}8714$). XGBoost vượt mục tiêu
($\mathrm{AUC}>0{,}87$) nhờ thiết kế đặc trưng có chủ đích, trong đó hai đặc trưng tự thiết
kế lọt vào top-3 theo giá trị SHAP. Ngưỡng quyết định được tối ưu theo $F_2$ tại
$t=0{,}625$, đạt Recall $66{,}9\%$ và giảm chi phí ước tính khoảng $2{,}01$ triệu USD so
với ngưỡng tối ưu theo $F_1$. Sản phẩm cuối là một ứng dụng Streamlit tương tác, hỗ trợ
dự báo đơn lẻ kèm giải thích SHAP và đánh giá theo lô.

\vspace{6pt}
\noindent\textbf{Từ khóa:} dự báo vỡ nợ, XGBoost, SHAP, $F_\beta$-score, dữ liệu mất cân bằng.

% ---------- MỤC LỤC ----------
\tableofcontents
\listoffigures

% ---------- BẢNG THUẬT NGỮ ----------
\chapter*{Bảng thuật ngữ viết tắt}
\addcontentsline{toc}{chapter}{Bảng thuật ngữ viết tắt}
\markboth{BẢNG THUẬT NGỮ VIẾT TẮT}{}

\begin{longtable}{>{\bfseries}p{3.2cm} p{11cm}}
\toprule
\textmd{\bfseries Thuật ngữ} & \textbf{Giải nghĩa}\\
\midrule
\endhead
AUC-ROC & Diện tích dưới đường cong ROC — đo khả năng xếp hạng. $0{,}5$ = ngẫu nhiên, $1$ = hoàn hảo.\\
TP/FP/TN/FN & Dương thật / Dương giả / Âm thật / Âm giả. FN đắt nhất trong tín dụng.\\
Precision/Recall & $\mathrm{Precision}=TP/(TP{+}FP)$; $\mathrm{Recall}=TP/(TP{+}FN)$.\\
$F_1/F_2$ & Trung bình điều hòa Precision--Recall; $F_2$ ưu tiên Recall gấp đôi Precision.\\
SHAP & Giá trị Shapley — phân tách dự báo thành đóng góp của từng đặc trưng.\\
Bagging/Boosting & Hai kiểu học tổ hợp: Bagging huấn luyện song song giảm phương sai; Boosting tuần tự giảm độ chệch.\\
Class Imbalance & Mất cân bằng lớp — lớp thiểu số chỉ chiếm $6{,}68\%$.\\
\texttt{scale\_pos\_weight} & Trọng số XGBoost nâng tầm lớp thiểu số.\\
\bottomrule
\end{longtable}

% =====================================================================
%  PHẦN THÂN (đánh số Ả Rập)
% =====================================================================
\cleardoublepage
\pagenumbering{arabic}

% =====================================================================
\chapter{Giới thiệu}\label{ch:gioithieu}

\section{Tính cấp thiết}
Tỷ lệ nợ xấu của hệ thống ngân hàng Việt Nam cuối năm 2023 ở mức $4{,}55\%$~\cite{sbv2024},
phản ánh áp lực rủi ro tín dụng ngày càng lớn. Quy trình thẩm định thủ công không mở rộng
được theo quy mô hồ sơ; trong khi các mô hình chấm điểm tuyến tính cổ điển bỏ qua các
tương tác phi tuyến giữa thu nhập, dư nợ và lịch sử trễ hạn.

Học máy học quy luật rủi ro từ dữ liệu lịch sử quy mô lớn và nắm bắt được các tương tác
phi tuyến này. Tuy nhiên, các mô hình mạnh thường mang tính ``hộp đen''. Hiệp ước Basel
và quy định bảo vệ dữ liệu (GDPR, Điều 22) yêu cầu tổ chức tín dụng phải \emph{giải thích
được} lý do từ chối một hồ sơ~\cite{bcbs2018,gdpr2018}. Phương pháp SHAP~\cite{lundberg2017}
giải quyết yêu cầu này bằng cách quy mỗi quyết định về đóng góp của từng đặc trưng.

\section{Mục tiêu (SMART)}
\begin{table}[htbp]\centering
\caption{Mục tiêu cụ thể và kết quả đạt được của đồ án.}
\label{tab:smart}
\begin{tabular}{p{4.2cm} p{5cm} p{4.2cm}}
\toprule
\textbf{Mục tiêu} & \textbf{Chỉ tiêu} & \textbf{Kết quả}\\
\midrule
Xây dựng mô hình & $\mathrm{AUC}\ge 0{,}87$ & \textbf{0,8714}\\
So sánh 4 thuật toán & Bảng đầy đủ & \cref{tab:sosanh}\\
Thiết kế đặc trưng & $\ge 2$ đặc trưng mới vào top-5 SHAP & \textbf{2 trong top-3}\\
Tối ưu ngưỡng & Tối ưu theo $F_2$ & $t=0{,}625$, Recall $66{,}9\%$\\
Demo sản phẩm & Streamlit + SHAP & Đạt\\
\bottomrule
\end{tabular}
\end{table}

\section{Phạm vi}
\begin{itemize}
  \item \textbf{Dữ liệu:} \emph{Give Me Some Credit} (Kaggle, 149.999 hồ sơ có nhãn trong
        \texttt{cs-training.csv}; \texttt{cs-test.csv} là tập nộp dự đoán, không có nhãn thật).
  \item \textbf{Mô hình:} bốn mô hình LR, DT, RF, XGBoost.
  \item \textbf{Không bao gồm:} mạng nơ-ron sâu, chấm điểm thời gian thực, tích hợp hệ thống.
  \item \textbf{Công nghệ:} Python 3.14, scikit-learn 1.8, XGBoost 3.2, SHAP 0.51.
\end{itemize}

\section{Đóng góp chính và quy trình tổng quan}
Đóng góp chính của đồ án gồm: (1) xây dựng đầy đủ quy trình từ khám phá dữ liệu đến demo
sản phẩm; (2) thiết kế 4 đặc trưng mới có ý nghĩa nghiệp vụ, 2 trong số đó vào top-3 SHAP;
(3) chuẩn hóa cách chọn ngưỡng triển khai theo $F_2$; (4) đóng gói thành ứng dụng web hỗ
trợ dự báo đơn lẻ và hậu kiểm theo lô. Toàn bộ quy trình được tóm tắt trong \cref{fig:pipeline}.

\begin{figure}[htbp]\centering
\begin{tikzpicture}[
  node distance=8mm and 7mm,
  box/.style={draw=bkblue,fill=boxgray,rounded corners=2pt,minimum height=10mm,
    text width=21mm,align=center,font=\small,thick},
  arr/.style={-{Latex[length=2.4mm]},bkblue,thick}]
  \node[box](a){Khám phá dữ liệu (EDA)};
  \node[box,right=of a](b){Tiền xử lý \& Khuyết thiếu};
  \node[box,right=of b](c){Thiết kế đặc trưng};
  \node[box,right=of c](d){Huấn luyện 4 mô hình};
  \node[box,below=of d](e){Đánh giá \& Tối ưu ngưỡng};
  \node[box,left=of e](f){Diễn giải SHAP};
  \node[box,left=of f](g){Hiệu chỉnh xác suất};
  \node[box,left=of g](h){Ứng dụng Streamlit};
  \draw[arr](a)--(b); \draw[arr](b)--(c); \draw[arr](c)--(d);
  \draw[arr](d)--(e); \draw[arr](e)--(f); \draw[arr](f)--(g); \draw[arr](g)--(h);
\end{tikzpicture}
\caption{Sơ đồ quy trình tổng quan của đồ án.}
\label{fig:pipeline}
\end{figure}

% =====================================================================
\chapter{Cơ sở lý thuyết}\label{ch:lythuyet}

Chương này trình bày nền tảng toán học của các mô hình và độ đo được sử dụng. Mục tiêu là
làm rõ \emph{vì sao} mỗi mô hình hoạt động, thay vì chỉ mô tả cách dùng.

\section{Bài toán phân loại nhị phân}
\begin{khungtheo}
\begin{dinhnghia}[Phân loại nhị phân]
Cho không gian đặc trưng $\mathcal{X}\subseteq\R^p$ và nhãn $y\in\{0,1\}$. Bài toán phân
loại nhị phân là tìm hàm $f:\mathcal{X}\to[0,1]$ ước lượng xác suất hậu nghiệm
$f(\xv)\approx \Prob(y=1\mid \xv)$. Quyết định cứng thu được qua một ngưỡng $t\in(0,1)$:
\[
  \hat{y}=\mathbf{1}\!\left[f(\xv)\ge t\right].
\]
\end{dinhnghia}
\end{khungtheo}

\paragraph{Mất cân bằng lớp.} Trong bộ dữ liệu, tỷ lệ vỡ nợ chỉ $6{,}68\%$, tức tỷ lệ
âm/dương xấp xỉ $14{:}1$. Hệ quả là (i) độ chính xác (accuracy) trở nên vô nghĩa — một bộ
phân loại luôn dự báo ``không vỡ nợ'' đã đạt $93{,}32\%$ accuracy; và (ii) ngưỡng tối ưu
thường khác xa $0{,}5$. Vì vậy ta đánh giá bằng AUC, $F_\beta$ và chi phí kỳ vọng thay vì
accuracy (xem \cref{sec:metric}).

\section{Hồi quy Logistic}\label{sec:lr}
Hồi quy Logistic mô hình hóa xác suất hậu nghiệm bằng hàm sigmoid
$\sigma(z)=\dfrac{1}{1+e^{-z}}$ áp lên một tổ hợp tuyến tính của đặc trưng:
\begin{equation}
  p_i \coloneqq \Prob(y_i=1\mid \xv_i)=\sigma(\wv^\top\xv_i + b)
  =\frac{1}{1+\exp\!\big(-(\wv^\top\xv_i+b)\big)}.
  \label{eq:lr}
\end{equation}
Tham số $(\wv,b)$ được ước lượng theo nguyên lý hợp lý cực đại (MLE). Với giả thiết các
quan sát độc lập tuân theo phân phối Bernoulli, hàm hợp lý là
$\prod_i p_i^{y_i}(1-p_i)^{1-y_i}$. Lấy logarit âm và chia cho $N$ ta được hàm mất mát
\emph{cross-entropy} (log-loss):
\begin{equation}
  \mathcal{L}(\wv,b)=-\frac{1}{N}\sum_{i=1}^{N}\Big[y_i\log p_i+(1-y_i)\log(1-p_i)\Big].
  \label{eq:logloss}
\end{equation}

\begin{menhde}[Gradient và tính lồi]\label{md:lr}
Đặt $X\in\R^{N\times p}$ là ma trận đặc trưng, $\mathbf{p}=(p_i)$, $\mathbf{y}=(y_i)$. Khi đó
\[
  \nabla_{\wv}\mathcal{L}=\frac{1}{N}X^\top(\mathbf{p}-\mathbf{y}),
  \qquad
  \nabla^2_{\wv}\mathcal{L}=\frac{1}{N}X^\top S X,
\]
với $S=\operatorname{diag}\big(p_i(1-p_i)\big)\succeq 0$. Do đó $\mathcal{L}$ là hàm lồi
theo $\wv$, và mọi điểm dừng đều là cực tiểu toàn cục.
\end{menhde}
\begin{proof}
Dùng $\sigma'(z)=\sigma(z)(1-\sigma(z))$. Với một quan sát, $\partial\mathcal{L}_i/\partial
\wv=-(y_i-p_i)\xv_i=(p_i-y_i)\xv_i$; lấy trung bình cho công thức gradient. Đạo hàm thêm
một lần: $\partial p_i/\partial\wv=p_i(1-p_i)\xv_i$, suy ra Hessian $=\frac1N\sum_i
p_i(1-p_i)\xv_i\xv_i^\top=\frac1N X^\top S X$. Với mọi $\mathbf{v}$,
$\mathbf{v}^\top X^\top S X\mathbf{v}=\sum_i p_i(1-p_i)(\xv_i^\top\mathbf{v})^2\ge 0$ vì
$p_i\in(0,1)$. Vậy Hessian nửa xác định dương, $\mathcal{L}$ lồi.
\end{proof}

\noindent\textbf{Ưu/nhược.} Nhanh, dễ giải thích (hệ số là log-odds), là baseline tốt;
nhưng chỉ tạo biên quyết định tuyến tính nên khó nắm bắt tương tác phi tuyến mạnh.

\section{Cây quyết định (CART)}\label{sec:dt}
Cây quyết định phân hoạch đệ quy không gian đặc trưng thành các vùng hình hộp, mỗi vùng
gán một dự báo. Tại mỗi nút, thuật toán CART chọn cặp (đặc trưng, ngưỡng) làm \emph{giảm
độ vẩn} (impurity) nhiều nhất. Với một nút $t$ chứa tỉ lệ lớp $p_{k\mid t}$, hai độ vẩn
thông dụng là
\begin{equation}
  \underbrace{G(t)=1-\sum_{k}p_{k\mid t}^2}_{\text{Gini}},
  \qquad
  \underbrace{H(t)=-\sum_{k}p_{k\mid t}\log_2 p_{k\mid t}}_{\text{Entropy}}.
  \label{eq:gini}
\end{equation}
Một phép chia $s$ tách nút cha thành nút con trái/phải với số mẫu $N_L,N_R$ ($N=N_L+N_R$).
Độ giảm độ vẩn (information gain) là
\begin{equation}
  \Delta I(s,t)=I(t)-\frac{N_L}{N}I(t_L)-\frac{N_R}{N}I(t_R),
  \label{eq:gain}
\end{equation}
và CART chọn $s^\star=\argmax_s \Delta I(s,t)$. Cây dừng khi đạt độ sâu tối đa hoặc số mẫu
lá tối thiểu.

\noindent\textbf{Ưu/nhược.} Trực quan, mô tả được logic quyết định; nhưng một cây đơn dễ
quá khớp (overfit) và có phương sai cao — đây chính là động lực cho mô hình tổ hợp.

\section{Bagging và Rừng ngẫu nhiên}\label{sec:rf}
Rừng ngẫu nhiên huấn luyện $B$ cây trên các mẫu bootstrap khác nhau và lấy trung bình dự
báo. Cơ chế giảm phương sai được làm rõ bởi kết quả sau.

\begin{menhde}[Giảm phương sai nhờ trung bình hóa]\label{md:rf}
Giả sử $B$ cây cho dự báo $\{Z_b\}$ cùng phân phối, mỗi cái có phương sai $\sigma^2$ và
tương quan từng cặp $\rho\ge 0$. Khi đó phương sai của dự báo trung bình
$\bar Z=\frac1B\sum_b Z_b$ là
\begin{equation}
  \operatorname{Var}(\bar Z)=\rho\,\sigma^2+\frac{1-\rho}{B}\,\sigma^2
  \xrightarrow{\;B\to\infty\;}\rho\,\sigma^2.
  \label{eq:rf}
\end{equation}
\end{menhde}
\begin{proof}
$\operatorname{Var}(\bar Z)=\frac{1}{B^2}\big[\sum_b\operatorname{Var}(Z_b)+\sum_{b\ne b'}
\operatorname{Cov}(Z_b,Z_{b'})\big]=\frac{1}{B^2}\big[B\sigma^2+B(B-1)\rho\sigma^2\big]
=\frac{\sigma^2}{B}+\frac{B-1}{B}\rho\sigma^2$, rút gọn ra \cref{eq:rf}.
\end{proof}

Số hạng thứ hai $\frac{1-\rho}{B}\sigma^2$ triệt tiêu khi tăng $B$, nhưng số hạng
$\rho\sigma^2$ thì không. Vì thế Rừng ngẫu nhiên còn \emph{lấy ngẫu nhiên một tập con đặc
trưng} tại mỗi phép chia để \emph{giảm $\rho$} giữa các cây — đó là điểm khác biệt cốt lõi
so với bagging thuần. Kết quả: ổn định hơn cây đơn, kháng nhiễu tốt, đổi lại khó diễn giải
trực tiếp.

\section{Gradient Boosting và XGBoost}\label{sec:xgb}
Khác với bagging (song song), boosting xây mô hình cộng dồn theo chuỗi: cây thứ $t$ sửa
phần sai số còn lại của tổ hợp $t-1$ cây trước. Dự báo sau $t$ vòng là
$\hat y_i^{(t)}=\hat y_i^{(t-1)}+f_t(\xv_i)$, với $f_t$ là một cây hồi quy. XGBoost cực
tiểu hàm mục tiêu \emph{có chính quy hóa}:
\begin{equation}
  \mathcal{L}^{(t)}=\sum_{i=1}^{N} l\!\big(y_i,\hat y_i^{(t-1)}+f_t(\xv_i)\big)
  +\Omega(f_t),
  \qquad
  \Omega(f)=\gamma T+\tfrac12\lambda\lVert\mathbf{w}\rVert^2,
  \label{eq:xgbobj}
\end{equation}
trong đó $T$ là số lá, $\mathbf{w}$ là véc-tơ trọng số lá, $\gamma,\lambda\ge 0$ kiểm soát
độ phức tạp. Khai triển Taylor bậc hai quanh $\hat y^{(t-1)}$ với
$g_i=\partial_{\hat y}l$, $h_i=\partial^2_{\hat y}l$:
\begin{equation}
  \mathcal{L}^{(t)}\simeq\sum_{i=1}^{N}\Big[g_i f_t(\xv_i)+\tfrac12 h_i f_t(\xv_i)^2\Big]
  +\Omega(f_t)+\text{const}.
  \label{eq:taylor}
\end{equation}

\begin{khungtheo}
\begin{dlytheo}[Trọng số lá tối ưu và độ lợi phép chia]\label{dl:xgb}
Với một cấu trúc cây cố định, gọi $I_j$ là tập mẫu rơi vào lá $j$, và đặt
$G_j=\sum_{i\in I_j}g_i$, $H_j=\sum_{i\in I_j}h_i$. Khi đó trọng số lá tối ưu và giá trị
mục tiêu tối ưu tương ứng là
\begin{equation}
  w_j^\star=-\frac{G_j}{H_j+\lambda},
  \qquad
  \tilde{\mathcal{L}}^{(t)}=-\frac12\sum_{j=1}^{T}\frac{G_j^2}{H_j+\lambda}+\gamma T.
  \label{eq:wopt}
\end{equation}
Độ lợi (gain) khi tách một lá thành hai lá trái/phải là
\begin{equation}
  \mathrm{Gain}=\frac12\!\left[\frac{G_L^2}{H_L+\lambda}+\frac{G_R^2}{H_R+\lambda}
  -\frac{(G_L+G_R)^2}{H_L+H_R+\lambda}\right]-\gamma.
  \label{eq:xgbgain}
\end{equation}
\end{dlytheo}
\end{khungtheo}
\begin{proof}
Vì một cây gán cùng một trọng số $w_j$ cho mọi mẫu trong lá $j$, nhóm tổng theo lá:
$\mathcal{L}^{(t)}=\sum_{j=1}^{T}\big[G_j w_j+\frac12(H_j+\lambda)w_j^2\big]+\gamma T$. Đây
là tam thức bậc hai theo $w_j$ với hệ số bậc hai dương; cực tiểu đạt tại
$w_j^\star=-G_j/(H_j+\lambda)$. Thế lại được
$\tilde{\mathcal{L}}^{(t)}=-\frac12\sum_j G_j^2/(H_j+\lambda)+\gamma T$. Độ lợi của phép
chia là chênh lệch giá trị mục tiêu trước và sau khi tách, cho ra \cref{eq:xgbgain}.
\end{proof}

\noindent Công thức \cref{eq:xgbgain} chính là tiêu chí XGBoost dùng để chọn điểm chia: nó
thưởng cho phép chia làm giảm mục tiêu và phạt $\gamma$ cho mỗi lá mới, tạo cơ chế cắt tỉa
tự nhiên. Việc dùng cả $g_i$ và $h_i$ (Newton bậc hai) khiến XGBoost hội tụ nhanh và ổn
định hơn boosting bậc nhất.

\section{Độ đo đánh giá}\label{sec:metric}
Từ ma trận nhầm lẫn $(TP,FP,FN,TN)$ định nghĩa
$\mathrm{Precision}=\frac{TP}{TP+FP}$, $\mathrm{Recall}=\frac{TP}{TP+FN}$. Tổng quát hóa
qua trung bình điều hòa có trọng số là $F_\beta$:
\begin{equation}
  F_\beta=(1+\beta^2)\cdot\frac{\mathrm{Precision}\cdot\mathrm{Recall}}
  {\beta^2\,\mathrm{Precision}+\mathrm{Recall}}.
  \label{eq:fbeta}
\end{equation}
Tham số $\beta$ đặt tầm quan trọng của Recall gấp $\beta$ lần Precision; ta chọn $\beta=2$
vì trong tín dụng, bỏ sót người vỡ nợ (FN) tốn kém hơn từ chối nhầm (FP).

\begin{khungtheo}
\begin{dlytheo}[Diễn giải xác suất của AUC]\label{dl:auc}
Gọi $\xv^+$ là một mẫu dương và $\xv^-$ là một mẫu âm lấy ngẫu nhiên độc lập. Khi đó diện
tích dưới đường cong ROC bằng xác suất mô hình xếp mẫu dương cao hơn mẫu âm:
\begin{equation}
  \mathrm{AUC}=\Prob\big(f(\xv^+)>f(\xv^-)\big)+\tfrac12\Prob\big(f(\xv^+)=f(\xv^-)\big).
  \label{eq:auc}
\end{equation}
\end{dlytheo}
\end{khungtheo}
\begin{proof}[Phác thảo]
Đường ROC là đồ thị $\mathrm{TPR}(t)$ theo $\mathrm{FPR}(t)$ khi quét ngưỡng $t$. Đổi biến,
$\mathrm{AUC}=\int_0^1 \mathrm{TPR}\,d(\mathrm{FPR})=\int \Prob(f(\xv^+)>t)\,
d\Prob(f(\xv^-)\le t)$, chính là $\Prob(f(\xv^+)>f(\xv^-))$ (cộng nửa khối lượng tại các
điểm bằng nhau). Đây cũng là thống kê $U$ Mann--Whitney chuẩn hóa, nên AUC bất biến với mọi
phép biến đổi đơn điệu của điểm số.
\end{proof}

\begin{nhanxet}[Ngưỡng Bayes tối ưu theo chi phí]\label{nx:bayes}
Nếu chi phí của một FP là $c_{FP}$ và của một FN là $c_{FN}$, quy tắc cực tiểu chi phí kỳ
vọng là dự báo dương khi $p>t^\star$ với
$t^\star=\dfrac{c_{FP}}{c_{FP}+c_{FN}}$. Chi phí FN càng lớn thì ngưỡng càng thấp.
\end{nhanxet}

\section{Diễn giải mô hình bằng SHAP}\label{sec:shap}
SHAP gán cho mỗi đặc trưng $i$ một đóng góp $\phi_i$ vào chênh lệch giữa dự báo và giá trị
nền, dựa trên \emph{giá trị Shapley} từ lý thuyết trò chơi hợp tác. Với hàm giá trị $v(S)$
là dự báo kỳ vọng khi chỉ biết tập đặc trưng $S$:
\begin{equation}
  \phi_i=\sum_{S\subseteq F\setminus\{i\}}\frac{|S|!\,(|F|-|S|-1)!}{|F|!}
  \big[v(S\cup\{i\})-v(S)\big].
  \label{eq:shap}
\end{equation}
Giá trị Shapley là cách phân bổ \emph{duy nhất} thỏa đồng thời bốn tiên đề: tính hiệu quả
($\sum_i\phi_i$ bằng chênh lệch dự báo), đối xứng, đặc trưng vô dụng có $\phi_i=0$, và tính
cộng tính. Với mô hình cây, \texttt{TreeExplainer} tính \cref{eq:shap} chính xác theo thời
gian đa thức, tránh chi phí tổ hợp $2^{|F|}$.

\section{Hiệu chỉnh xác suất và kiểm định thống kê}\label{sec:calib}
Điểm số của cây boosting thường không phải xác suất hiệu chỉnh tốt. \emph{Platt scaling}
hiệu chỉnh bằng một hồi quy logistic một biến trên điểm số: $\hat p=\sigma(a\,f(\xv)+b)$.
Chất lượng hiệu chỉnh đo bằng điểm Brier $\mathrm{BS}=\frac1N\sum_i(\hat p_i-y_i)^2$ và sai
số hiệu chỉnh kỳ vọng ECE. Để kiểm tra chênh lệch AUC giữa hai mô hình \emph{tương quan}
(cùng tập kiểm tra) có ý nghĩa thống kê hay không, ta dùng kiểm định DeLong.

% =====================================================================
\chapter{Dữ liệu và Tiền xử lý}\label{ch:dulieu}

\section{Khám phá dữ liệu (EDA)}
Bộ dữ liệu gồm 149.999 hồ sơ (sau khi loại 1 dòng có tuổi $=0$), 10 đặc trưng gốc. Tỷ lệ
vỡ nợ $6{,}68\%$ ($9.951$ dương / $140.048$ âm) — mất cân bằng mạnh
(\cref{fig:target}). Một số biến tài chính như \texttt{RevolvingUtilization} và
\texttt{DebtRatio} có phân phối lệch phải nặng, cần lưu ý các giá trị cực trị
(\cref{fig:univariate}).

\begin{figure}[htbp]\centering
\includegraphics[width=0.7\linewidth]{../reports/fig_01_target_distribution.png}
\caption{Phân bố lớp mục tiêu — mất cân bằng $\approx 14{:}1$.}
\label{fig:target}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.95\linewidth]{../reports/fig_03_univariate.png}
\caption{Phân phối đơn biến của các đặc trưng gốc.}
\label{fig:univariate}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_05_correlation_heatmap.png}
\caption{Ma trận tương quan giữa các đặc trưng.}
\label{fig:corr}
\end{figure}

Nhóm biến trễ hạn (\texttt{30-59}, \texttt{60-89}, \texttt{90+} ngày) liên hệ mạnh nhất với
rủi ro vỡ nợ (\cref{fig:delinq}), gợi ý giá trị của các đặc trưng tổng hợp từ nhóm này.

\begin{figure}[htbp]\centering
\includegraphics[width=0.9\linewidth]{../reports/fig_07_delinquency_analysis.png}
\caption{Phân tích nhóm biến trễ hạn theo tỷ lệ vỡ nợ.}
\label{fig:delinq}
\end{figure}

\section{Xử lý giá trị thiếu}
Hai biến có khuyết thiếu đáng kể: \texttt{MonthlyIncome} ($19{,}8\%$) và số người phụ thuộc.
Ta dùng KNN Imputer ($k=5$): mỗi giá trị thiếu được điền bằng trung bình của 5 hồ sơ gần
nhất theo khoảng cách Euclid trên các chiều quan sát được. So với điền trung vị, KNN giữ
được cấu trúc tương quan giữa các biến (\cref{fig:imputation}).

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_11_imputation_comparison.png}
\caption{So sánh phân phối trước/sau khi điền khuyết bằng KNN.}
\label{fig:imputation}
\end{figure}

\section{Chuẩn hóa}
Áp dụng \texttt{StandardScaler} cho Hồi quy Logistic để gradient descent ổn định (đưa các
đặc trưng về cùng thang đo). Các mô hình dựa trên cây (DT, RF, XGBoost) bất biến với phép
biến đổi đơn điệu nên không cần chuẩn hóa.

\section{Thiết kế đặc trưng}
Bốn đặc trưng được thiết kế dựa trên hiểu biết nghiệp vụ:
\begin{enumerate}
  \item \texttt{TotalDelinquencyScore} $=3\times(90+)+2\times(60\text{-}89)+1\times(30\text{-}59)$ — phạt nặng trễ hạn kéo dài.
  \item \texttt{FinancialStressIndex} $=\texttt{RevolvingUtil}\times\texttt{TotalDelinquencyScore}$ — dùng hết hạn mức \emph{và} trễ hạn = áp lực cao.
  \item \texttt{AbsoluteMonthlyDebt} $=\texttt{DebtRatio}\times\texttt{MonthlyIncome}$ — quy dư nợ tương đối về giá trị tuyệt đối.
  \item \texttt{DelinquencySeverityBalance} $=(30\text{-}59)-(90+)$ — cân bằng mức độ trễ hạn trên ảnh chụp tĩnh.
\end{enumerate}
Hiệu quả của thiết kế này được kiểm chứng ở \cref{sec:shap_kq}.

\section{Mất cân bằng lớp}
Để xử lý mất cân bằng, ta dùng \texttt{scale\_pos\_weight} $=n_-/n_+\approx 14$ trong
XGBoost (nâng trọng số lớp thiểu số) và khảo sát SMOTE (sinh mẫu thiểu số tổng hợp,
\cref{fig:smote}). Cuối cùng chọn trọng số lớp vì giữ được phân phối gốc và tránh rò rỉ
dữ liệu khi kết hợp với kiểm định chéo.

\begin{figure}[htbp]\centering
\includegraphics[width=0.8\linewidth]{../reports/fig_15_smote.png}
\caption{Minh họa SMOTE sinh mẫu lớp thiểu số.}
\label{fig:smote}
\end{figure}

% =====================================================================
\chapter{Thực nghiệm và Đánh giá}\label{ch:thucnghiem}

\section{Thiết kế thử nghiệm}
Chia dữ liệu theo tỷ lệ $70/15/15$: huấn luyện $104.999$, kiểm định $22.500$, kiểm tra
$22.500$ hồ sơ, dùng phân tầng (stratified) để giữ tỷ lệ lớp. Lựa chọn siêu tham số bằng
kiểm định chéo phân tầng $k=5$.

\section{So sánh mô hình}
XGBoost cho AUC cao nhất và thỏa mục tiêu $\mathrm{AUC}\ge 0{,}87$ (\cref{tab:sosanh},
\cref{fig:compare}). Đường ROC/PR của bốn mô hình được so sánh trong \cref{fig:roc}.

\begin{table}[htbp]\centering
\caption{Kết quả tổng hợp bốn mô hình trên tập kiểm tra.}
\label{tab:sosanh}
\begin{tabular}{l c c c c c}
\toprule
\textbf{Mô hình} & \textbf{AUC-ROC} & \textbf{F1} & \textbf{Recall} & \textbf{Precision} & \textbf{Thời gian (s)}\\
\midrule
Logistic Regression & 0,8432 & 0,398 & 41,2\% & 39,8\% & 1,23\\
Decision Tree (CART) & 0,8579 & 0,421 & 48,7\% & 40,1\% & 0,18\\
Random Forest & 0,8703 & 0,437 & 52,1\% & 40,5\% & 5,32\\
\textbf{XGBoost} & \textbf{0,8714} & \textbf{0,442} & \textbf{52,6\%} & \textbf{41,3\%} & \textbf{2,18}\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]\centering
\includegraphics[width=0.92\linewidth]{../reports/fig_20_model_comparison.png}
\caption{So sánh tổng hợp các chỉ số giữa bốn mô hình.}
\label{fig:compare}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.92\linewidth]{../reports/fig_roc_comparison.png}
\caption{Đường ROC của bốn mô hình trên tập kiểm tra.}
\label{fig:roc}
\end{figure}

\section{Diễn giải SHAP}\label{sec:shap_kq}
Áp dụng \texttt{TreeExplainer} cho XGBoost. Hai đặc trưng tự thiết kế
(\texttt{FinancialStressIndex}, \texttt{TotalDelinquencyScore}) lọt vào top-3
(\cref{tab:shap}, \cref{fig:shapbar}), xác nhận hiệu quả của thiết kế đặc trưng.

\begin{table}[htbp]\centering
\caption{Năm đặc trưng quan trọng nhất theo $|\text{SHAP}|$ trung bình.}
\label{tab:shap}
\begin{tabular}{c l c}
\toprule
\textbf{Hạng} & \textbf{Đặc trưng} & \textbf{SHAP}\\
\midrule
1 & RevolvingUtilizationOfUnsecuredLines & 0,251\\
2 & \textit{FinancialStressIndex} (tự thiết kế) & 0,198\\
3 & \textit{TotalDelinquencyScore} (tự thiết kế) & 0,187\\
4 & NumberOfOpenCreditLinesAndLoans & 0,142\\
5 & Age & 0,108\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]\centering
\includegraphics[width=0.9\linewidth]{../reports/fig_26a_shap_bar.png}
\caption{Tầm quan trọng đặc trưng theo giá trị SHAP.}
\label{fig:shapbar}
\end{figure}

\section{Tối ưu ngưỡng theo \texorpdfstring{$F_2$}{F2}}
Theo Nhận xét~\ref{nx:bayes}, ngưỡng Bayes với $c_{FP}=500$, $c_{FN}=11.250$ là
$t^\star=\frac{500}{500+11.250}\approx 0{,}043$ — quá thấp (từ chối $\sim 74\%$ hồ sơ),
không thực dụng. Thay vào đó ta tối ưu trực tiếp $F_2$ (\cref{eq:fbeta}, $\beta=2$):
quét $t\in[0{,}05;0{,}95]$ cho cực đại tại $t=0{,}625$ với Recall $66{,}9\%$, Precision
$29{,}9\%$, $F_2=0{,}537$ (\cref{tab:nguong}, \cref{fig:threshold}).

\begin{table}[htbp]\centering
\caption{So sánh các ngưỡng trên tập kiểm tra (22.500 hồ sơ).}
\label{tab:nguong}
\begin{tabular}{l c c c c}
\toprule
\textbf{Ngưỡng} & \textbf{Recall} & \textbf{Precision} & \textbf{$F_2$} & \textbf{Chi phí (tr.USD)}\\
\midrule
Mặc định (0,50) & 77,5\% & 22,2\% & 0,518 & 5,84\\
\textbf{$F_2$-optimal (0,625)} & \textbf{66,9\%} & \textbf{29,9\%} & \textbf{0,537} & \textbf{6,78}\\
$F_1$-optimal (0,77) & 51,6\% & 39,4\% & 0,486 & 8,79\\
Bayes-optimal (0,043) & 99,2\% & 6,9\% & 0,265 & 34,50\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]\centering
\includegraphics[width=0.9\linewidth]{../reports/fig_25_threshold_optimization.png}
\caption{Tối ưu ngưỡng vận hành theo $F_2$.}
\label{fig:threshold}
\end{figure}

So với ngưỡng $F_1$-optimal, ngưỡng $F_2$ giảm khoảng $2{,}01$ triệu USD chi phí trong khi
vẫn ưu tiên phát hiện hồ sơ rủi ro.

\section{Phân tích lỗi và hiệu chỉnh xác suất}
Phần lớn lỗi tập trung ở vùng điểm rủi ro sát ngưỡng — các hồ sơ có tín hiệu mâu thuẫn
(\cref{fig:score}); vùng này phù hợp để chuyển sang rà soát thủ công. Sau Platt scaling,
điểm Brier giảm từ $0{,}0582$ xuống $0{,}0478$ và ECE từ $3{,}2\%$ xuống $1{,}9\%$, tức xác
suất dự báo gần thực tế hơn (\cref{fig:calib}). Kiểm định DeLong xác nhận chênh lệch AUC
giữa XGBoost và Random Forest là nhỏ và không có ý nghĩa thống kê mạnh, nhất quán với
\cref{tab:sosanh}.

\begin{figure}[htbp]\centering
\includegraphics[width=0.8\linewidth]{../reports/fig_24_score_distribution.png}
\caption{Phân bố điểm rủi ro của hai lớp quanh ngưỡng quyết định.}
\label{fig:score}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.8\linewidth]{../reports/fig_31_calibration.png}
\caption{Đường hiệu chỉnh xác suất trước và sau Platt scaling.}
\label{fig:calib}
\end{figure}

% =====================================================================
\chapter{Sản phẩm — Ứng dụng Streamlit}\label{ch:sanpham}

\section{Kiến trúc hệ thống}
Ứng dụng được đóng gói thành một web app Streamlit với kiến trúc tách lớp như
\cref{fig:arch}: lớp giao diện nhận đầu vào, lớp xử lý nạp mô hình đã lưu và bộ tiền xử lý,
lớp giải thích sinh biểu đồ SHAP. Mô hình và \texttt{TreeExplainer} được nạp một lần và lưu
đệm để tối ưu hiệu năng.

\begin{figure}[htbp]\centering
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  box/.style={draw=bkblue,fill=boxgray,rounded corners=2pt,minimum height=11mm,
    text width=26mm,align=center,font=\small,thick},
  db/.style={draw=bkred,fill=white,rounded corners=2pt,minimum height=11mm,
    text width=22mm,align=center,font=\small,thick},
  arr/.style={-{Latex[length=2.4mm]},bkblue,thick}]
  \node[box](ui){Giao diện Streamlit\\(2 tab)};
  \node[box,right=of ui](pre){Bộ tiền xử lý\\(scaler, imputer)};
  \node[box,right=of pre](mdl){Mô hình XGBoost\\\texttt{best\_model.pkl}};
  \node[box,below=of mdl](shap){SHAP\\TreeExplainer};
  \node[box,left=of shap](out){Kết quả + Giải thích};
  \draw[arr](ui)--(pre); \draw[arr](pre)--(mdl); \draw[arr](mdl)--(shap);
  \draw[arr](shap)--(out); \draw[arr](out)-|(ui);
\end{tikzpicture}
\caption{Kiến trúc lớp của ứng dụng Streamlit.}
\label{fig:arch}
\end{figure}

\section{Tính năng chính}
\begin{itemize}
  \item \textbf{Tab 1 — Dự báo đơn lẻ:} nhập thông tin khách hàng, hiển thị xác suất vỡ nợ
        và quyết định (Duyệt/Từ chối) kèm biểu đồ SHAP waterfall giải thích.
  \item \textbf{Tab 2 — Đánh giá theo lô:} tải lên CSV/XLSX, dự báo theo lô, cho chọn
        ngưỡng (mặc định $0{,}625$). Nếu file có cột nhãn thật \texttt{SeriousDlqin2yrs},
        ứng dụng tính AUC, Recall, Precision, $F_2$, ROC/PR, ma trận nhầm lẫn và tác động
        chi phí, đồng thời tóm tắt theo phân khúc thu nhập và nhóm tuổi.
\end{itemize}
Ứng dụng demo: \url{https://doandanhlong-loan-prediction.streamlit.app}.

\section{Chạy thử hậu kiểm theo lô}
Để kiểm tra chức năng hậu kiểm, đồ án tạo \texttt{batch\_demo\_from\_training\_5000.csv}
(5.000 hồ sơ, 334 vỡ nợ, giữ tỷ lệ $6{,}68\%$). Kết quả tại ngưỡng $t=0{,}625$ cho trong
\cref{tab:batch}; phân bố và ma trận nhầm lẫn ở \cref{fig:batch}.

\begin{table}[htbp]\centering
\caption{Kết quả hậu kiểm theo lô tại ngưỡng $t=0{,}625$.}
\label{tab:batch}
\begin{tabular}{l r}
\toprule
\textbf{Chỉ số} & \textbf{Giá trị}\\
\midrule
AUC-ROC & 0,8780\\
Recall & 68,26\%\\
Precision & 29,53\%\\
$F_2$-score & 0,5408\\
TP / FP / FN / TN & 228 / 544 / 106 / 4122\\
Tỷ lệ từ chối & 15,44\%\\
Chi phí tại ngưỡng & 1.464.500 USD\\
Chi phí nếu duyệt tất cả & 3.757.500 USD\\
Tiết kiệm mô phỏng & 2.293.000 USD\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]\centering
\includegraphics[width=0.49\linewidth]{../reports/fig_34_batch_demo_confusion.png}\hfill
\includegraphics[width=0.49\linewidth]{../reports/fig_35_batch_demo_cost.png}
\caption{Ma trận nhầm lẫn (trái) và tác động chi phí (phải) trên demo theo lô.}
\label{fig:batch}
\end{figure}

\noindent\textbf{Lưu ý:} đây là mô phỏng hậu kiểm bằng dữ liệu huấn luyện có nhãn. Trong
vận hành thật, nhãn thật chỉ xuất hiện sau kỳ quan sát. Không dùng tập kiểm tra Kaggle để
hậu kiểm vì tập này không có nhãn thật.

\section{Kết quả thử nộp Kaggle}
Thử nộp đạt Public AUC $=0{,}85785$ và Private AUC $=0{,}86482$, khá gần AUC nội bộ
$0{,}8714$ — cho thấy mô hình tổng quát hóa ổn trên tập đánh giá ngoài (\cref{fig:kaggle}).

\begin{figure}[htbp]\centering
\includegraphics[width=0.8\linewidth]{../reports/fig_36_kaggle_submission_result.png}
\caption{Kết quả nộp thử trên Kaggle.}
\label{fig:kaggle}
\end{figure}

% =====================================================================
\chapter{Kết luận và Hướng phát triển}\label{ch:ketluan}

\section{Kết luận}
\begin{enumerate}
  \item XGBoost đạt AUC $=0{,}8714$, vượt mục tiêu $0{,}87$.
  \item Thiết kế đặc trưng có chủ đích: 2 đặc trưng tự thiết kế vào top-3 SHAP.
  \item Ngưỡng $F_2$-optimal ($0{,}625$) cân bằng Recall--Precision phù hợp tín dụng và
        giảm chi phí so với ngưỡng $F_1$.
  \item SHAP giải thích từng quyết định, đáp ứng yêu cầu minh bạch (Basel III, GDPR).
\end{enumerate}

\section{Hướng phát triển}
\begin{itemize}
  \item \textbf{Dữ liệu:} kết hợp dữ liệu tín dụng Việt Nam (CIC, SBV); chấm điểm thời gian thực.
  \item \textbf{Mô hình:} mạng nơ-ron có cơ chế attention; mô hình hóa chuỗi thời gian (LSTM); meta-learner.
  \item \textbf{Giải thích:} giải thích phản thực (counterfactual); hồ sơ tương tự (prototype).
  \item \textbf{Vận hành:} thử nghiệm A/B cho ngưỡng; giám sát trôi dữ liệu; vòng phản hồi cập nhật mô hình.
\end{itemize}

% =====================================================================
%  TÀI LIỆU THAM KHẢO
% =====================================================================
\begin{thebibliography}{99}
\addcontentsline{toc}{chapter}{Tài liệu tham khảo}
\bibitem{sbv2024} Ngân hàng Nhà nước Việt Nam (2024). \emph{Báo cáo tình hình ngân hàng 2023}. \url{https://www.sbv.gov.vn}.
\bibitem{bcbs2018} Basel Committee on Banking Supervision (2018). \emph{Basel III: Finalising post-crisis reforms}. Bank for International Settlements.
\bibitem{lundberg2017} Lundberg, S. M., \& Lee, S. I. (2017). A unified approach to interpreting model predictions. \emph{NeurIPS}.
\bibitem{chen2016} Chen, T., \& Guestrin, C. (2016). XGBoost: A scalable tree boosting system. \emph{KDD}.
\bibitem{pedregosa2011} Pedregosa, F., et al. (2011). Scikit-learn: Machine learning in Python. \emph{JMLR}, 12, 2825--2830.
\bibitem{kaggle2011} Kaggle (2011). \emph{Give Me Some Credit}. \url{https://www.kaggle.com/c/GiveMeSomeCredit}.
\bibitem{breiman2001} Breiman, L. (2001). Random Forests. \emph{Machine Learning}, 45(1), 5--32.
\bibitem{delong1988} DeLong, E. R., DeLong, D. M., \& Clarke-Pearson, D. L. (1988). Comparing areas under two or more correlated ROC curves. \emph{Biometrics}, 44(3), 837--845.
\bibitem{platt1999} Platt, J. (1999). Probabilistic outputs for support vector machines. \emph{Advances in Large Margin Classifiers}.
\bibitem{gdpr2018} European Union (2018). \emph{General Data Protection Regulation (EU) 2016/679}.
\bibitem{molnar2020} Molnar, C. (2020). \emph{Interpretable Machine Learning: A Guide for Making Black Box Models Explainable}.
\bibitem{hastie2009} Hastie, T., Tibshirani, R., \& Friedman, J. (2009). \emph{The Elements of Statistical Learning} (2nd ed.). Springer.
\end{thebibliography}

% =====================================================================
%  PHỤ LỤC
% =====================================================================
\appendix
\renewcommand{\chaptername}{Phụ lục}
\titleformat{\chapter}[hang]
  {\Huge\bfseries\color{bkblue}}
  {\colorbox{bkblue}{\color{white}\rule[-6pt]{0pt}{28pt}\,\thechapter\,}}{16pt}{\Huge}

\chapter{Siêu tham số XGBoost}
\begin{verbatim}
{
  'n_estimators': 200,      'max_depth': 6,
  'learning_rate': 0.05,    'subsample': 0.8,
  'colsample_bytree': 0.8,  'scale_pos_weight': 14.05,  # n_neg/n_pos
  'objective': 'binary:logistic',  'random_state': 42
}
\end{verbatim}

\chapter{Hình kỹ thuật bổ sung}
Các hình dưới đây phục vụ kiểm tra sâu hơn, không lặp lại hình ở phần chính.

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_14_mutual_information.png}
\caption{Thông tin tương hỗ giữa đặc trưng và nhãn.}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_27_shap_dependence.png}
\caption{Đồ thị phụ thuộc SHAP cho đặc trưng chính.}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_29_learning_curve.png}
\caption{Đường cong học của XGBoost.}
\end{figure}

\begin{figure}[htbp]\centering
\includegraphics[width=0.85\linewidth]{../reports/fig_32_delong_summary.png}
\caption{Tóm tắt kiểm định DeLong giữa các mô hình.}
\end{figure}

\end{document}
